[
    {
        "name": "query_local_imessage_history",
        "description": "Queries the local macOS chat.db SQLite database to find historical iMessage threads, contacts, or context.",
        "parameters": {
            "type": "object",
            "properties": {
                "search_term": {"type": "string"},
                "limit": {"type": "integer", "default": 50}
            },
            "required": ["search_term"]
        }
    },
    {
        "name": "semantic_local_file_search",
        "description": "Uses embeddings to search the local filesystem (Documents, Pictures) for files matching a semantic concept rather than exact filenames.",
        "parameters": {
            "type": "object",
            "properties": {
                "concept": {"type": "string"},
                "file_types": {"type": "array", "items": {"type": "string"}},
                "limit": {"type": "integer", "default": 5}
            },
            "required": ["concept"]
        }
    },
    {
        "name": "adobe_suite_expert_injection",
        "description": "Executes dynamic JavaScript payload inside Adobe Creative Cloud (Photoshop, Illustrator, Premiere) via Apple Events/ExtendScript.",
        "parameters": {
            "type": "object",
            "properties": {
                "target_app": {"type": "string", "enum": ["Photoshop", "Illustrator", "Premiere", "AfterEffects"]},
                "action": {"type": "string"},
                "asset_path": {"type": "string", "description": "Absolute path to a local asset to manipulate or inject."}
            },
            "required": ["target_app", "action"]
        }
    },
    {
        "name": "final_cut_pro_genius_edit",
        "description": "Compiles a valid FCPXML 1.9 file and physically opens it in Final Cut Pro to instantly generate video timelines.",
        "parameters": {
            "type": "object",
            "properties": {
                "action": {"type": "string", "description": "The style of edit, e.g., 'assemble_hype_trailer'"},
                "assets": {"type": "array", "items": {"type": "string"}, "description": "Array of absolute file paths to video/audio assets."},
                "timeline_name": {"type": "string"}
            },
            "required": ["action", "assets", "timeline_name"]
        }
    },
    {
        "name": "discover_system_tools",
        "description": "Lists all available allowlisted tools, system utilities, and CLI binaries installed in the VM",
        "parameters": {
            "type": "object",
            "properties": {
                "category": {
                    "type": "string",
                    "enum": ["gui", "terminal", "filesystem", "browser", "all"],
                    "description": "Category of tools to inspect"
                }
            },
            "required": ["category"]
        }
    },
    {
        "name": "capture_screen",
        "description": "Captures the current display state for multimodal model reasoning.",
        "parameters": {
            "type": "object",
            "properties": {}
        }
    },
    {
        "name": "mouse_click",
        "description": "Moves the cursor to absolute coordinates (x, y) and performs a click.",
        "parameters": {
            "type": "object",
            "properties": {
                "x": {"type": "number", "description": "The absolute X coordinate on the screen"},
                "y": {"type": "number", "description": "The absolute Y coordinate on the screen"},
                "button": {"type": "string", "enum": ["left", "right"], "description": "Which mouse button to click"}
            },
            "required": ["x", "y"]
        }
    },
    {
        "name": "keyboard_type",
        "description": "Types a string into the frontmost application as if typed by a human.",
        "parameters": {
            "type": "object",
            "properties": {
                "text": {"type": "string", "description": "The exact text to type"}
            },
            "required": ["text"]
        }
    }
]
